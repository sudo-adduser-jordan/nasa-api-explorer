'use client';
import styles from './marsinfo.module.css';

export default function MarsInfo() {
    const content = (
        <>
            <section className={styles.container}>section</section>
        </>
    );
    return content;
}
